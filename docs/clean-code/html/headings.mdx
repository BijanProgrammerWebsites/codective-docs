---
description: ""
---

import HtmlHeadingsComponent from "@site/src/components/common-mistakes/html-headings/html-headings.component.tsx";

# Headings

## تعریف

تو HTML به تگ‌های `h1` تا `h6` میگن Headings.

## کاربرد

برخلاف چیزی که اکثر دانشجوها فکر می‌کنن، نباید از این تگ‌ها صرفاً به خاطر ظاهرشون استفاده کنید.
یعنی نباید پیش خودتون بگید چون می‌خوام این نوشته خیلی بزرگ باشه پس از `h1` استفاده کنم.

به جاش باید به خاطر معنی‌شون ازشون استفاده کنید.
برای اینکه بهتر متوجه کارکرد این تگ‌ها بشید، فرض کنید سایتتون مثل یه کتابه.

### کاربرد h1

همون‌طور که روی جلد کتاب اسمش رو می‌نویسن و جای دیگه تکرار نمی‌شه،
از تگ `h1` هم فقط باید یک بار تو کل سایت استفاده کنید.
متن داخل این تگ باید جوری باشه که با خوندنش بفهمیم این سایت در مورد چیه.

### کاربرد h2

برای نشون‌دادن فصل‌های کتاب، از تگ‌های `h2` استفاده می‌کنیم.
بنابراین هر فصل باید دقیقاً یه دونه `h2` داشته باشه.

### کاربرد h3

اگه فصل‌مون به بخش‌های مختلف تقسیم شده باشه،
عنوان هر زیرفصل رو با `h3` مشخص می‌کنیم.

### کاربرد باقی تگ‌ها

و به صورت مشابه برای تگ‌های `h4` تا `h6` ماجرا رو ادامه می‌دیم.

## مثال

به عنوان مثال برای همین صفحه، می‌تونیم این کد رو در نظر بگیریم:

<HtmlHeadingsComponent toc={[{ id: "1", level: 1, value: "Headings" }, ...toc]} />
