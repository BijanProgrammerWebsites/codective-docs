name: Deploy
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    uses: BijanProgrammerWebsites/hub/.github/workflows/deploy-docusaurus.yml@master
    with:
      host: docs.codective.ir
      base-href: /
    secrets: inherit
